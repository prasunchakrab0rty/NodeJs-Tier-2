import { SpecialToSpacePipe } from './special-to-space.pipe';

describe('SpecialToSpacePipe', () => {
  it('create an instance', () => {
    const pipe = new SpecialToSpacePipe();
    expect(pipe).toBeTruthy();
  });
});
